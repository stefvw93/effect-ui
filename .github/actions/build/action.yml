name: Build
description: Build workspace packages

runs:
  using: composite
  steps:
      - name: Setup
        uses: stefvw93/effect-ui/.github/actions/setup@main

      - name: Build workspace packages
        shell: bash
        run: pnpm build
